

===== SCRIPT =====


===== SCRIPT =====

const SECURE_KEY="controle_financeiro_secure_v1";const LEGACY_KEY="controle_financeiro_v1";const PREF="controle_financeiro_notificacoes";let data=[];let unlocked=false;let inactivityTimer=null;const INACTIVITY_MS=10*60*1000;
async function deriveKey(password,salt){const material=await crypto.subtle.importKey("raw",new TextEncoder().encode(password),{name:"PBKDF2"},false,["deriveKey"]);return crypto.subtle.deriveKey({name:"PBKDF2",salt,iterations:250000,hash:"SHA-256"},material,{name:"AES-GCM",length:256},false,["encrypt","decrypt"])}
function b64(input){const arr=input instanceof Uint8Array?input:new Uint8Array(input);let out="";for(let i=0;i<arr.length;i+=0x8000)out+=String.fromCharCode.apply(null,arr.subarray(i,i+0x8000));return btoa(out)}function bytes(s){return Uint8Array.from(atob(s),c=>c.charCodeAt(0))}
async function encryptData(password,obj){const salt=crypto.getRandomValues(new Uint8Array(16)),iv=crypto.getRandomValues(new Uint8Array(12));const key=await deriveKey(password,salt);const ct=await crypto.subtle.encrypt({name:"AES-GCM",iv},key,new TextEncoder().encode(JSON.stringify(obj)));return {v:1,salt:b64(salt),iv:b64(iv),data:b64(new Uint8Array(ct))}}
const VAULT_B64=/^[A-Za-z0-9+/]+={0,2}$/;function parseVault(raw){let blob=raw;if(typeof blob==="string"){try{blob=JSON.parse(blob)}catch(e){return null}}if(!blob||typeof blob!=="object")return null;const salt=blob.salt,iv=blob.iv,payload=blob.data;if(typeof salt!=="string"||typeof iv!=="string"||typeof payload!=="string")return null;if(!salt||!iv||!payload)return null;if(!VAULT_B64.test(salt)||!VAULT_B64.test(iv)||!VAULT_B64.test(payload))return null;return {v:blob.v||1,salt:salt,iv:iv,data:payload}}
async function decryptData(password,raw){const blob=parseVault(raw);if(!blob)throw new Error("VAULT_FORMAT");const key=await deriveKey(password,bytes(blob.salt));const pt=await crypto.subtle.decrypt({name:"AES-GCM",iv:bytes(blob.iv)},key,bytes(blob.data));return JSON.parse(new TextDecoder().decode(pt))}
async function decryptVault(password,raw){if(!parseVault(raw))return {ok:false,reason:"format"};try{const value=await decryptData(password,raw);return {ok:true,value:Array.isArray(value)?value:[]}}catch(e){return {ok:false,reason:e&&e.message==="VAULT_FORMAT"?"format":"password"}}
function queueSync(){if(typeof syncNow!=="function")return;Promise.resolve().then(()=>syncNow(false)).catch(()=>{})}async function secureSave(){if(!unlocked)return;const blob=await encryptData(window.__financePassword,data);localStorage.setItem(SECURE_KEY,JSON.stringify(blob));localStorage.removeItem(LEGACY_KEY)}
let securityMode="setup";function setSecurityMode(mode){securityMode=mode;const p2=document.getElementById("unlockPassword2"),title=document.getElementById("lockTitle"),desc=document.getElementById("lockDescription"),btn=document.getElementById("unlockButton"),cloudBtn=document.getElementById("lockCloudBtn");if(!p2||!title||!desc||!btn)return;const setup=mode==="setup",restore=mode==="restore";p2.style.display=setup?"block":"none";p2.required=setup;title.textContent=setup?"Criar proteção":restore?"Restaurar dados da nuvem":"Aplicativo bloqueado";desc.textContent=setup?"Crie uma senha para proteger os dados financeiros registrados neste aplicativo.":restore?"Esta conta já possui lançamentos guardados na nuvem. Digite a senha de proteção usada no outro dispositivo para baixá-los.":"Digite sua senha para acessar seus dados financeiros.";btn.textContent=setup?"Criar proteção":restore?"Restaurar dados":"Desbloquear";if(cloudBtn)cloudBtn.style.display=(setup||restore)?"block":"none"}
function showLock(mode="unlock"){const screen=document.getElementById("lockScreen");screen.classList.remove("hidden");document.getElementById("unlockPassword").value="";document.getElementById("unlockPassword2").value="";document.getElementById("lockMsg").textContent="";setSecurityMode(mode);setTimeout(()=>document.getElementById("unlockPassword").focus(),50)}function clearPasswordInputs(){["unlockPassword","unlockPassword2","syncRecoverPassword"].forEach(id=>{const el=document.getElementById(id);if(el)el.value=""})}function hideLock(){clearPasswordInputs();document.getElementById("lockScreen").classList.add("hidden")}function resetInactivity(){if(!unlocked)return;clearTimeout(inactivityTimer);inactivityTimer=setTimeout(()=>lockApp(),INACTIVITY_MS)}function lockApp(){if(!unlocked)return;window.__financePassword=null;unlocked=false;clearTimeout(inactivityTimer);showLock("unlock")}["click","touchstart","keydown"].forEach(ev=>document.addEventListener(ev,resetInactivity,{passive:true}));
let localLegacyData=[];async function initializeSecurity(){const stored=localStorage.getItem(SECURE_KEY);let legacyData=[];try{const legacy=localStorage.getItem(LEGACY_KEY);legacyData=legacy?JSON.parse(legacy):[]}catch(e){legacyData=[]}localLegacyData=Array.isArray(legacyData)?legacyData:[];const form=document.getElementById("unlockForm"),password=document.getElementById("unlockPassword"),password2=document.getElementById("unlockPassword2"),msg=document.getElementById("lockMsg");form.onsubmit=async e=>{e.preventDefault();msg.textContent="";const p=password.value;if(p.length<6){msg.textContent="Use uma senha com pelo menos 6 caracteres.";return}if(securityMode==="restore"){msg.textContent="Baixando dados da conta...";let raw=null;try{raw=await cloudFetchVault()}catch(err){msg.textContent="Não foi possível baixar os dados da conta agora. Verifique a conexão e tente de novo.";return}if(!raw){setSecurityMode(localStorage.getItem(SECURE_KEY)?"unlock":"setup");msg.textContent="Nenhum dado encontrado na nuvem para esta conta.";return}const res=await decryptVault(p,raw);if(!res.ok){msg.textContent=res.reason==="format"?"Os dados guardados na nuvem estão em um formato que esta versão não reconhece. Nada foi alterado.":"Senha de proteção incorreta para os dados desta conta.";password.value="";password.focus();return}const local=localLegacyData;data=(local.length&&typeof mergeRecords==="function")?mergeRecords(res.value,local):res.value;window.__financePassword=p;unlocked=true;try{await secureSave()}catch(err){}hideLock();render();resetInactivity();queueSync();return}if(securityMode==="setup"){const p2=password2.value;if(p!==p2){msg.textContent="As senhas não coincidem.";return}window.__financePassword=p;data=localLegacyData;try{unlocked=true;await secureSave();hideLock();render();resetInactivity();queueSync()}catch(err){window.__financePassword=null;unlocked=false;msg.textContent="Não foi possível proteger os dados neste navegador."}return}const raw=localStorage.getItem(SECURE_KEY);if(!raw){setSecurityMode("setup");msg.textContent="Crie a proteção deste dispositivo para começar.";return}const res=await decryptVault(p,raw);if(!res.ok){msg.textContent=res.reason==="format"?"Os dados protegidos deste navegador estão inválidos ou incompletos.":"Senha incorreta.";password.value="";password.focus();return}data=res.value;window.__financePassword=p;unlocked=true;hideLock();render();resetInactivity();queueSync()};if(stored)showLock("unlock");else showLock("setup")}
const cats={entrada:["Vendas","Eventos","Salário","Transferência recebida","Outros"],saida:["Ingredientes","Bebidas","Embalagens","Energia","Água","Internet","Aluguel","Funcionários","Transporte","Manutenção","Marketing","Impostos","Compras","Transferência/Retirada","Outros"]};const brl=v=>new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(v||0);
function today(){const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`}
async function save(){await secureSave();if(typeof syncNow==="function")await syncNow(false)}function fmtDate(s){return s?new Date(s+"T12:00:00").toLocaleDateString("pt-BR"):"—"}function daysUntil(s){return Math.ceil((new Date(s+"T12:00:00")-new Date(today()+"T12:00:00"))/86400000)}function statusClass(x){return x.status==="pago"?"paid":daysUntil(x.vencimento)<0?"overdue":"pending"}function statusText(x){if(x.status==="pago")return x.tipo==="entrada"?"Recebido":"Pago";if(x.vencimento&&daysUntil(x.vencimento)<0)return "Vencido";return "Pendente"}
function updateCats(){let el=document.getElementById("categoria"),old=el.value;el.innerHTML=cats[document.getElementById("tipo").value].map(x=>`<option>${x}</option>`).join("");if(cats[document.getElementById("tipo").value].includes(old))el.value=old}function updateStatusOptions(){let s=document.getElementById("status"),old=s.value,entrada=document.getElementById("tipo").value==="entrada";s.innerHTML=entrada?'<option value="pago">Recebido</option><option value="pendente">Pendente</option>':'<option value="pago">Pago</option><option value="pendente">Pendente</option>';s.value=old==="pendente"?"pendente":"pago"}document.getElementById("tipo").addEventListener("change",()=>{updateCats();updateStatusOptions()});updateCats();updateStatusOptions();document.getElementById("data").value=today();
document.querySelectorAll("nav button").forEach(b=>b.onclick=()=>{document.querySelectorAll("nav button").forEach(x=>x.classList.remove("active"));b.classList.add("active");document.querySelectorAll(".view").forEach(x=>x.classList.remove("active"));document.getElementById(b.dataset.view).classList.add("active");render()});
document.getElementById("form").onsubmit=async e=>{e.preventDefault();let base={id:crypto.randomUUID(),tipo:tipo.value,controle:controle.value,descricao:descricao.value,categoria:categoria.value,valor:Number(valor.value),data:dataEl(),vencimento:vencimento.value||dataEl(),forma:forma.value,status:document.getElementById("status").value==="pendente"?"pendente":"pago",recorrencia:recorrencia.value};let qtd=Math.max(1,Math.min(60,Number(quantidade.value)||1));for(let i=0;i<qtd;i++){let x={...base,id:crypto.randomUUID()};if(i>0){let d=new Date(base.vencimento+"T12:00:00");if(base.recorrencia==="mensal")d.setMonth(d.getMonth()+i);if(base.recorrencia==="semanal")d.setDate(d.getDate()+7*i);if(base.recorrencia==="anual")d.setFullYear(d.getFullYear()+i);x.vencimento=toInputDate(d);let bd=new Date(base.data+"T12:00:00");if(base.recorrencia==="mensal")bd.setMonth(bd.getMonth()+i);if(base.recorrencia==="semanal")bd.setDate(bd.getDate()+7*i);if(base.recorrencia==="anual")bd.setFullYear(bd.getFullYear()+i);x.data=toInputDate(bd);x.status="pendente"}data.push(stampRecord(x))}await save();alert("Lançamento salvo com sucesso.");limparForm();render()};
function dataEl(){return document.getElementById("data").value}function toInputDate(d){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`}function limparForm(){document.getElementById("form").reset();document.getElementById("data").value=today();document.getElementById("quantidade").value=1;updateCats();updateStatusOptions();document.getElementById("status").value="pago";updateStatusOptions()}
function activeData(){return data.filter(x=>x&&!x.deleted)}
function filtered(){const ini=fDataIni.value,fim=fDataFim.value;return activeData().filter(x=>(!fControle.value||x.controle===fControle.value)&&(!fTipo.value||x.tipo===fTipo.value)&&(!fStatus.value||x.status===fStatus.value)&&(!ini||x.data>=ini)&&(!fim||x.data<=fim)).sort((a,b)=>b.data.localeCompare(a.data))}
[fControle,fTipo,fStatus,fDataIni,fDataFim].forEach(x=>x.onchange=render);
function openNewFor(controleValor){limparForm();document.getElementById("controle").value=controleValor;document.getElementById("status").value="pago";updateStatusOptions();document.querySelectorAll(".view").forEach(x=>x.classList.remove("active"));document.querySelectorAll("nav button").forEach(x=>x.classList.remove("active"));document.getElementById("lancamento").classList.add("active");document.querySelector('nav button[data-view="lancamento"]').classList.add("active");window.scrollTo({top:0,behavior:"smooth"})}
function homeCard(label,value,cls){return `<div class="card"><div class="label">${label}</div><div class="value ${cls}">${brl(value)}</div></div>`}
function render(){let now=new Date(),month=`${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,"0")}`;const calc=(arr,t,c)=>arr.filter(x=>x.status==="pago"&&x.tipo===t&&(!c||x.controle===c)).reduce((s,x)=>s+x.valor,0);let ep=calc(data,"entrada","pessoal"),sp=calc(data,"saida","pessoal"),er=calc(data,"entrada","restaurante"),sr=calc(data,"saida","restaurante");let pMes=activeData().filter(x=>x.data.startsWith(month)&&x.controle==="pessoal"),rMes=activeData().filter(x=>x.data.startsWith(month)&&x.controle==="restaurante");let pRec=pMes.filter(x=>x.tipo==="entrada"&&x.status==="pendente").reduce((s,x)=>s+x.valor,0),pPag=pMes.filter(x=>x.tipo==="saida"&&x.status==="pendente").reduce((s,x)=>s+x.valor,0),rRec=rMes.filter(x=>x.tipo==="entrada"&&x.status==="pendente").reduce((s,x)=>s+x.valor,0),rPag=rMes.filter(x=>x.tipo==="saida"&&x.status==="pendente").reduce((s,x)=>s+x.valor,0);cards.innerHTML=`<div class="home-section"><div class="home-section-title">👤 FINANÇAS PESSOAIS</div><div class="grid">${homeCard("Saldo atual",ep-sp,ep-sp>=0?"green":"red")}${homeCard("Entradas do mês",calc(pMes,"entrada"),"green")}${homeCard("Saídas do mês",calc(pMes,"saida"),"red")}${homeCard("A receber",pRec,"orange")}${homeCard("A pagar",pPag,"orange")}</div></div><div class="home-section"><div class="home-section-title">🍽️ FINANÇAS DO RESTAURANTE</div><div class="grid">${homeCard("Saldo atual",er-sr,er-sr>=0?"green":"red")}${homeCard("Entradas do mês",calc(rMes,"entrada"),"green")}${homeCard("Saídas do mês",calc(rMes,"saida"),"red")}${homeCard("A receber",rRec,"orange")}${homeCard("A pagar",rPag,"orange")}</div><button class="mobile-new primary" onclick="openNewFor('restaurante')">＋ Novo lançamento (Restaurante)</button></div>`;let due=activeData().filter(x=>x.status==="pendente"&&x.vencimento&&daysUntil(x.vencimento)>=0&&daysUntil(x.vencimento)<=7).sort((a,b)=>a.vencimento.localeCompare(b.vencimento)),overdue=activeData().filter(x=>x.status==="pendente"&&x.vencimento&&daysUntil(x.vencimento)<0).sort((a,b)=>b.vencimento.localeCompare(a.vencimento));homeDue.innerHTML=due.length?due.map(dueRow).join(""):'<div class="empty">Nenhuma conta vencendo nos próximos 7 dias.</div>';homeOverdue.innerHTML=overdue.length?overdue.map(dueRow).join(""):'<div class="empty">Nenhuma conta vencida.</div>';tbody.innerHTML=filtered().map(x=>`<tr><td>${fmtDate(x.data)}</td><td>${x.descricao}</td><td>${x.tipo==="entrada"?"Entrada":"Saída"}</td><td>${x.controle==="pessoal"?"Pessoal":"Restaurante"}</td><td>${x.categoria}</td><td class="${x.tipo==="entrada"?"green":"red"}">${x.tipo==="entrada"?"+":"-"} ${brl(x.valor)}</td><td><span class="status ${statusClass(x)}">${statusText(x)}</span></td><td class="actions"><button class="btn-edit" onclick="editItem('${x.id}')">✏️ Editar</button>${x.status==="pendente"?`<button class="btn-pay" onclick="pay('${x.id}')">${x.tipo==="entrada"?"Recebido":"Pago"}</button>`:""}<button class="btn-delete" onclick="delItem('${x.id}')">🗑️</button></td></tr>`).join("")||`<tr><td colspan="8" class="empty">Nenhuma movimentação encontrada.</td></tr>`;let m=activeData().filter(x=>x.data.startsWith(month)),en=m.filter(x=>x.tipo==="entrada").reduce((s,x)=>s+x.valor,0),sa=m.filter(x=>x.tipo==="saida").reduce((s,x)=>s+x.valor,0);reportCards.innerHTML=[["Entradas",en,"green"],["Saídas",sa,"red"],["Resultado",en-sa,en-sa>=0?"green":"red"],["Contas pendentes",activeData().filter(x=>x.status==="pendente").reduce((s,x)=>s+x.valor,0),"orange"]].map(x=>`<div class="card"><div class="label">${x[0]}</div><div class="value ${x[2]}">${brl(x[1])}</div></div>`).join("");let map={};m.forEach(x=>{map[x.categoria]??={e:0,s:0};map[x.categoria][x.tipo==="entrada"?"e":"s"]+=x.valor});catBody.innerHTML=Object.entries(map).map(([k,v])=>`<tr><td>${k}</td><td class="green">${brl(v.e)}</td><td class="red">${brl(v.s)}</td></tr>`).join("")||'<tr><td colspan="3" class="empty">Sem dados no mês atual.</td></tr>';dueList.innerHTML=activeData().filter(x=>x.status==="pendente"&&x.vencimento).sort((a,b)=>a.vencimento.localeCompare(b.vencimento)).map(d=>`<div class="card" style="margin-bottom:8px"><div class="actions"><strong>${d.descricao}</strong><span class="status ${statusClass(d)}">${statusText(d)}</span><span style="margin-left:auto">${brl(d.valor)}</span></div><div class="label">Vencimento: ${fmtDate(d.vencimento)} · ${d.controle} · ${d.categoria}</div><div class="actions" style="margin-top:8px"><button class="success" onclick="pay('${d.id}')">☑ Marcar como pago</button></div></div>`).join("")||'<div class="empty">Nenhum vencimento pendente.</div>';loadPrefs()}
function dueRow(d){let n=daysUntil(d.vencimento),txt=n<0?`vencida há ${Math.abs(n)} dia(s)`:n===0?"vence hoje":`vence em ${n} dia(s)`;return `<div style="padding:9px 0;border-bottom:1px solid var(--line)"><strong>${d.descricao}</strong> — ${brl(d.valor)} · ${fmtDate(d.vencimento)} <span class="label">(${txt})</span></div>`}async function pay(id){let x=activeData().find(x=>x.id===id);if(x){x.status="pago";stampRecord(x);await save();render()}}
function editItem(id){let x=activeData().find(y=>y.id===id);if(!x)return;document.getElementById("editId").value=x.id;document.getElementById("eTipo").value=x.tipo;document.getElementById("eControle").value=x.controle;document.getElementById("eDescricao").value=x.descricao;updateEditCats(x.categoria);document.getElementById("eValor").value=x.valor;document.getElementById("eData").value=x.data;document.getElementById("eVencimento").value=x.vencimento||"";document.getElementById("eForma").value=x.forma||"Outro";updateEditStatus(x.status);document.getElementById("editModal").classList.add("show")}
function updateEditCats(selected){let el=document.getElementById("eCategoria");el.innerHTML=cats[document.getElementById("eTipo").value].map(x=>`<option>${x}</option>`).join("");if(selected)el.value=selected}function updateEditStatus(selected){let s=document.getElementById("eStatus"),entrada=document.getElementById("eTipo").value==="entrada";s.innerHTML=entrada?'<option value="pago">Recebido</option><option value="pendente">Pendente</option>':'<option value="pago">Pago</option><option value="pendente">Pendente</option>';s.value=selected||"pago"}document.getElementById("eTipo")?.addEventListener("change",()=>{updateEditCats();updateEditStatus(document.getElementById("eStatus").value)});function closeEdit(){document.getElementById("editModal").classList.remove("show")}
async function delItem(id){if(!confirm("Excluir este lançamento permanentemente?"))return;let existed=false;const kept=[];for(const x of data){if(x&&x.id===id){if(!x.deleted)existed=true;continue}kept.push(x)}data=kept;if(existed)data.push({id:id,deleted:true,deletedAt:new Date().toISOString()});await save();render()}document.getElementById("eTipo")?.addEventListener("change",()=>updateEditCats());
function loadPrefs(){let p=JSON.parse(localStorage.getItem(PREF)||'{"7":true,"3":true,"1":true,"0":true,"over":true}');n7.checked=p["7"];n3.checked=p["3"];n1.checked=p["1"];n0.checked=p["0"];nover.checked=p.over}["n7","n3","n1","n0","nover"].forEach(id=>document.getElementById(id).onchange=()=>{localStorage.setItem(PREF,JSON.stringify({"7":n7.checked,"3":n3.checked,"1":n1.checked,"0":n0.checked,over:nover.checked}))});document.getElementById("editForm").onsubmit=async e=>{e.preventDefault();let x=activeData().find(y=>y.id===document.getElementById("editId").value);if(!x)return;x.tipo=eTipo.value;x.controle=eControle.value;x.descricao=eDescricao.value;x.categoria=eCategoria.value;x.valor=Number(eValor.value);x.data=eData.value;x.vencimento=eVencimento.value||eData.value;x.forma=eForma.value;x.status=eStatus.value;stampRecord(x);await save();closeEdit();render();alert("Alterações salvas com sucesso.")};
function notifications(){let p=JSON.parse(localStorage.getItem(PREF)||'{"7":true,"3":true,"1":true,"0":true,"over":true}'),msgs=[];activeData().filter(x=>x.status==="pendente"&&x.vencimento).forEach(x=>{let n=daysUntil(x.vencimento);if((n===7&&p["7"])||(n===3&&p["3"])||(n===1&&p["1"])||(n===0&&p["0"])||(n<0&&p.over))msgs.push(`${x.descricao}: ${n<0?"vencida":n===0?"vence hoje":`vence em ${n} dias`} — ${brl(x.valor)}`)});if(msgs.length&&"Notification"in window&&Notification.permission==="granted")new Notification("Meu Controle Financeiro",{body:msgs.slice(0,3).join("\n")})}if("Notification"in window&&Notification.permission==="default")Notification.requestPermission().catch(()=>{});initializeSecurity().then(()=>{const v=document.getElementById("appVersion");if(v)v.textContent="V1.14";queueSync()});


===== SCRIPT =====

let __supabase=null,__syncReady=false,__syncBusy=false,__cloudMismatch=false;function syncConfigured(){return !!(window.SUPABASE_URL&&window.SUPABASE_PUBLISHABLE_KEY&&!window.SUPABASE_URL.includes("COLOQUE")&&!window.SUPABASE_PUBLISHABLE_KEY.includes("COLOQUE"))}function setSyncState(state){const d=document.getElementById("syncDot");if(!d)return;d.className="sync-dot "+(state||"")}function openSyncModal(){document.getElementById("syncModal").classList.add("show");const msg=document.getElementById("syncMsg");msg.textContent=syncConfigured()?"":"A sincronização ainda precisa ser configurada no arquivo config.js.";refreshSyncUI()}function closeSyncModal(){document.getElementById("syncModal").classList.remove("show")}function setRecoverVisible(v){const el=document.getElementById("syncRecover");if(el)el.style.display=v?"block":"none"}function syncErrorText(e){const m=e&&e.message?String(e.message):"";return m||"Verifique a configuração e a conexão."}
function refreshSyncUI(){const logged=document.getElementById("syncLogged");if(!__supabase){logged.style.display="none";return}__supabase.auth.getUser().then(({data:res})=>{const user=res&&res.user;setRecoverVisible(!!user&&unlocked&&__cloudMismatch);if(user){logged.style.display="block";document.getElementById("syncUser").textContent="Conta conectada: "+user.email;document.getElementById("syncIntro").textContent="Sua conta está conectada. Os lançamentos podem ser sincronizados entre seus dispositivos."}else{logged.style.display="none";document.getElementById("syncIntro").textContent="Entre com sua conta para usar os mesmos lançamentos no celular e no computador."}})}
async function cloudVaultRow(columns){const {data:res,error:userErr}=await __supabase.auth.getUser();const user=res&&res.user;if(userErr||!user)throw new Error("Sessão inválida");const {data:row,error}=await __supabase.from("finance_vault").select(columns||"payload,updated_at").eq("user_id",user.id).maybeSingle();if(error)throw error;return {user:user,row:row||null}}async function cloudFetchVault(){if(!__supabase||!__syncReady)throw new Error("Sem sessão");const {row}=await cloudVaultRow("payload");return row?row.payload:null}async function maybeOfferCloudRestore(){if(unlocked)return false;if(localStorage.getItem(SECURE_KEY))return false;if(!__supabase||!__syncReady)return false;try{const {row}=await cloudVaultRow("user_id,updated_at");if(!row)return false;setSecurityMode("restore");return true}catch(e){return false}}async function pushMerged(user,remoteValue){const merged=mergeRecords(Array.isArray(data)?data:[],Array.isArray(remoteValue)?remoteValue:[]);data=merged;const payload=await encryptData(window.__financePassword,data);const {error}=await __supabase.from("finance_vault").upsert({user_id:user.id,payload},{onConflict:"user_id"});if(error)throw error;await secureSave();render()}
async function initCloud(){if(!syncConfigured()){setSyncState("err");return}try{__supabase=window.supabase.createClient(window.SUPABASE_URL,window.SUPABASE_PUBLISHABLE_KEY,{auth:{autoRefreshToken:true,persistSession:true,detectSessionInUrl:true}});const {data:{session}}=await __supabase.auth.getSession();__syncReady=!!session;setSyncState(session?"ok":"");__supabase.auth.onAuthStateChange((_event,newSession)=>{__syncReady=!!newSession;setSyncState(newSession?"ok":"");if(!newSession){__cloudMismatch=false;setRecoverVisible(false)}refreshSyncUI();if(!newSession)return;if(unlocked)syncNow(false);else maybeOfferCloudRestore()});refreshSyncUI();if(session){if(unlocked)await syncNow(false);else await maybeOfferCloudRestore()}}catch(e){console.warn("Falha ao iniciar sincronização",e);setSyncState("err")}}
async function syncLogin(){if(!syncConfigured()){document.getElementById("syncMsg").textContent="Configure primeiro o arquivo config.js.";return}const email=document.getElementById("syncEmail").value.trim(),password=document.getElementById("syncPassword").value,msg=document.getElementById("syncMsg");msg.textContent="";if(!email||!password){msg.textContent="Informe e-mail e senha.";return}setSyncState("busy");const {error}=await __supabase.auth.signInWithPassword({email,password});if(error){setSyncState("err");msg.textContent="Não foi possível entrar. Verifique os dados da conta.";return}setSyncState("ok");document.getElementById("syncPassword").value="";if(!unlocked){const offered=await maybeOfferCloudRestore();msg.textContent=offered?"Conta conectada. Informe a senha de proteção na tela inicial para baixar os lançamentos.":"Conta conectada. Desbloqueie o aplicativo para sincronizar.";refreshSyncUI();if(offered)closeSyncModal();return}msg.textContent="Conta conectada. Sincronizando...";await syncNow(true);refreshSyncUI()}
async function syncSignup(){if(!syncConfigured()){document.getElementById("syncMsg").textContent="Configure primeiro o arquivo config.js.";return}const email=document.getElementById("syncEmail").value.trim(),password=document.getElementById("syncPassword").value,msg=document.getElementById("syncMsg");msg.textContent="";if(!email||password.length<6){msg.textContent="Informe um e-mail e uma senha com pelo menos 6 caracteres.";return}setSyncState("busy");const {data,error}=await __supabase.auth.signUp({email,password});if(error){setSyncState("err");msg.textContent="Não foi possível criar a conta. Verifique os dados.";return}setSyncState("ok");document.getElementById("syncPassword").value="";msg.textContent=data.session?"Conta criada. Sincronizando...":"Conta criada. Verifique o e-mail de confirmação e depois entre no aplicativo.";if(data.session){if(unlocked)await syncNow(true);else await maybeOfferCloudRestore()}refreshSyncUI()}
async function syncLogout(){if(!__supabase)return;await __supabase.auth.signOut();__syncReady=false;__cloudMismatch=false;setRecoverVisible(false);setSyncState("");refreshSyncUI()}
async function syncNow(manual=false){const msg=document.getElementById("syncMsg");if(!syncConfigured()){if(manual&&msg)msg.textContent="A sincronização ainda precisa ser configurada no arquivo config.js.";return}if(!__supabase||!__syncReady){if(manual&&msg)msg.textContent="Entre na sua conta para sincronizar.";return}if(!unlocked){if(manual&&msg)msg.textContent="Desbloqueie o aplicativo com a senha de proteção antes de sincronizar.";return}if(__syncBusy)return;__syncBusy=true;setSyncState("busy");try{const {user,row}=await cloudVaultRow("payload,updated_at");if(!row){const payload=await encryptData(window.__financePassword,data);const {error}=await __supabase.from("finance_vault").insert({user_id:user.id,payload});if(error)throw error;__cloudMismatch=false;setRecoverVisible(false);setSyncState("ok");if(manual&&msg){msg.textContent="Sincronização concluída.";refreshSyncUI()}return}const res=await decryptVault(window.__financePassword,row.payload);if(!res.ok){__cloudMismatch=true;setRecoverVisible(true);setSyncState("err");if(msg){msg.textContent=res.reason==="format"?"Os dados guardados na nuvem estão em um formato que esta versão não reconhece. Nada foi apagado nem sobrescrito.":"Os lançamentos já guardados na nuvem foram protegidos com outra senha de proteção. Informe abaixo a senha anterior para uni-los — nada será apagado."}refreshSyncUI();return}__cloudMismatch=false;setRecoverVisible(false);await pushMerged(user,res.value);setSyncState("ok");if(manual&&msg){msg.textContent="Sincronização concluída.";refreshSyncUI()}}catch(e){setSyncState("err");if(manual&&msg)msg.textContent="Não foi possível sincronizar. "+syncErrorText(e)}finally{__syncBusy=false}}
async function recoverCloudVault(){const input=document.getElementById("syncRecoverPassword"),msg=document.getElementById("syncMsg");if(!__supabase||!__syncReady){msg.textContent="Entre na sua conta primeiro.";return}if(!unlocked){msg.textContent="Desbloqueie o aplicativo antes de unir os dados.";return}const previous=input?input.value:"";if(!previous){msg.textContent="Informe a senha de proteção usada anteriormente.";return}if(__syncBusy)return;__syncBusy=true;setSyncState("busy");msg.textContent="Verificando a senha informada...";try{const {user,row}=await cloudVaultRow("payload");if(!row){__cloudMismatch=false;setRecoverVisible(false);setSyncState("ok");msg.textContent="Não há dados na nuvem para unir.";return}const res=await decryptVault(previous,row.payload);if(!res.ok){setSyncState("err");msg.textContent=res.reason==="format"?"Os dados da nuvem estão em um formato não reconhecido. Nada foi alterado.":"A senha informada não abre os dados guardados na nuvem. Nada foi alterado.";return}await pushMerged(user,res.value);__cloudMismatch=false;setRecoverVisible(false);setSyncState("ok");msg.textContent="Dados da nuvem unidos e reprotegidos com a senha de proteção atual deste dispositivo."}catch(e){setSyncState("err");msg.textContent="Não foi possível unir os dados agora. "+syncErrorText(e)}finally{if(input)input.value="";__syncBusy=false;refreshSyncUI()}}
const TOMBSTONE_TTL_MS=180*24*60*60*1000;function stampRecord(x){if(x&&typeof x==="object")x.updatedAt=new Date().toISOString();return x}function recordTime(x){if(!x)return 0;const t=Date.parse((x.deleted?x.deletedAt:x.updatedAt)||"");return isNaN(t)?0:t}function mergeRecords(a,b){const limit=Date.now()-TOMBSTONE_TTL_MS;const vigente=x=>{if(!x||!x.id)return false;if(!x.deleted)return true;const t=Date.parse(x.deletedAt||"");return isNaN(t)?true:t>=limit};const map=new Map(),removed=new Map();[...b,...a].filter(vigente).forEach(x=>{if(x.deleted){removed.set(x.id,{id:x.id,deleted:true,deletedAt:x.deletedAt||new Date().toISOString()});return}const cur=map.get(x.id);if(!cur||recordTime(x)>=recordTime(cur))map.set(x.id,x)});removed.forEach((v,k)=>map.set(k,v));return Array.from(map.values())}window.addEventListener("online",()=>{if(unlocked)syncNow(false)});
initCloud();if("serviceWorker"in navigator){window.addEventListener("load",async()=>{try{const registration=await navigator.serviceWorker.register("./sw.js",{scope:"./"});if(registration.waiting)showUpdateNotice(registration);registration.addEventListener("updatefound",()=>{const worker=registration.installing;if(!worker)return;worker.addEventListener("statechange",()=>{if(worker.state==="installed"&&navigator.serviceWorker.controller)showUpdateNotice(registration)})})}catch(err){console.warn("Service Worker não pôde ser registrado:",err)}})}function showUpdateNotice(registration){if(document.getElementById("updateNotice"))return;const box=document.createElement("div");box.id="updateNotice";box.style.cssText="position:fixed;left:14px;right:14px;bottom:78px;z-index:90;background:#fff;border:1px solid #ddd;border-radius:12px;padding:12px 14px;box-shadow:0 8px 30px #0002;display:flex;gap:10px;align-items:center;justify-content:space-between;font-size:13px";box.innerHTML='<span>🔄 Há uma nova versão disponível.</span><button class="primary" style="padding:8px 12px" onclick="applyUpdate()">Atualizar</button>';document.body.appendChild(box);window.__pendingSW=registration.waiting}async function applyUpdate(){const sw=window.__pendingSW;if(sw)sw.postMessage({type:"SKIP_WAITING"});else location.reload()}if("serviceWorker"in navigator){navigator.serviceWorker.addEventListener("controllerchange",()=>location.reload())}
